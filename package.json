{
  "name": "workspace",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "preinstall": "sh -c 'rm -f package-lock.json yarn.lock; case \"$npm_config_user_agent\" in pnpm/*) ;; *) echo \"Use pnpm instead\" >&2; exit 1 ;; esac'",
    "build": "pnpm run typecheck && pnpm -r --if-present run build",
    "build:deploy": "pnpm run build && node ./scripts/copy-client.mjs",
    "start": "node ./artifacts/api-server/dist/index.mjs",
    "db:push": "pnpm --filter @workspace/scripts run migrate-orders && pnpm --filter @workspace/db run push-force",
    "db:seed": "pnpm --filter @workspace/scripts run seed-products",
    "typecheck:libs": "tsc --build",
    "typecheck": "pnpm run typecheck:libs && pnpm -r --filter \"./artifacts/**\" --filter \"./scripts\" --if-present run typecheck"
  },
  "packageManager": "pnpm@11.12.0",
  "engines": {
    "node": ">=22"
  },
  "private": true,
  "dependencies": {
    "@replit/connectors-sdk": "^0.4.1"
  },
  "devDependencies": {
    "prettier": "^3.9.4",
    "typescript": "~5.9.3"
  }
}
